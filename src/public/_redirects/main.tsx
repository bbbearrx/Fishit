# ==========================================
# CLOUDFLARE PAGES SPA REDIRECTS
# ==========================================
# This file configures Cloudflare Pages to serve the React Router SPA correctly.
# All routes should be handled by index.html for client-side routing.

# Redirect all paths to index.html for SPA routing (200 = rewrite, not redirect)
/*    /index.html   200
